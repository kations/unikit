---
name: Inputs
route: /Inputs
menu: UI
---

import {useRef} from 'react'
import { Playground, Props } from 'docz'
import { Input, Flex, Form, Button } from '../index'

# Inputs

### Usage

```bash
import { Input } from 'unikit'
```

## Input 

<Playground>
 {() => {
    const formRef = useRef(null)
    return (
      <>
      <Button onPress={() => alert(formRef.current.submit())}>Custom trigger</Button>
        <Flex p={30} bg="background">
            <Form ref={formRef} onSubmit={(doc) => alert(JSON.stringify(doc))} buttonProps={{mt: 10}}>
                <Flex p={30} type="background">
                <Input key="text.bla" type="text" label="Text" clean={false} required />
                <Input key="text.blub" type="textarea" label="Text" required />
                </Flex>
                <Input key="range" type="range" label="Slider" />
                <Input key="tags" type="tags" label="Tags" value={['Tag']} />
                <Input key="switch" type="switch" label="Switch"  />
                <Input key="select" type="select" label="Select" options={['One', 'Two']}  />
                <Input key="multi" type="multiselect" label="MultiSelect" options={['One', 'Two']}  />
                <Input key="number" type="number" label="Number"  />
                <Input key="checkbox" type="checkbox" label="Checkbox"  />
                <Input key="date" type="date" label="DatePicker"  />
                <Input key="color" type="color" label="Color"  />
            </Form>
    </Flex>
     </>
    )
  }}
</Playground>

### Props

<Props of={Input} />

## Slider 

<Playground>
    <Flex p={30} type="background">
        <Input.Slider value={30} showValue />
        <Input.Slider value={30} mt={50} sliderHeight={300} showValue vertical />
        <Input.Slider value={30} steps={10} mt={50} onChange={value => console.log(value)} />
    </Flex>
</Playground>
