---
name: Inputs
route: /Inputs
menu: UI
---

import {useRef} from 'react'
import { Playground, Props } from 'docz'
import { Input, Flex, Form, Button } from '../index'

# Inputs

### Usage

```bash
import { Input } from 'unikit'
```

## Input 

<Playground>
 {() => {
    const formRef = useRef(null)
    return (
      <>
      <Button onPress={() => formRef.current.submit()}>Custom trigger</Button>
        <Flex p={30} bg="background">
            <Form ref={formRef} onSubmit={(doc, reset) => {
              alert(JSON.stringify(doc))
              reset()
              }} buttonProps={{mt: 10}}>
                <Flex p={30} type="background">
                <Input field="text.bla" type="text" label="Text" clean={false} placeholder="placeholder" required />
                <Input field="text.blub" type="textarea" label="Text" required />
                </Flex>
                <Input field="range" type="range" label="Slider" />
                <Input field="tags" type="tags" label="Tags" value={['Tag']} />
                <Input field="switch" type="switch" label="Switch"  />
                <Input field="select" type="select" label="Select" options={['One', 'Two']}  />
                <Input field="multi" type="multiselect" label="MultiSelect" options={['One', 'Two']}  />
                <Input field="number" type="number" label="Number"  />
                <Input field="checkbox" type="checkbox" label="Checkbox"  />
                <Input field="date" type="date" label="DatePicker"  />
                <Input field="color" type="color" label="Color"  />
            </Form>
    </Flex>
     </>
    )
  }}
</Playground>

### Props

<Props of={Input} />

## Slider 

<Playground>
    <Flex p={30} type="background">
        <Input.Slider value={30} showValue />
        <Input.Slider value={30} mt={50} sliderHeight={300} showValue vertical />
        <Input.Slider value={30} steps={10} mt={50} onChange={value => console.log(value)} />
    </Flex>
</Playground>
