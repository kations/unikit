---
name: Tabs
route: /Tabs
menu: UI
---

import { Playground, Props } from 'docz'
import {Flex, Box, Tabs} from '../index'

# Tabs

### Usage

```bash
import { Tabs } from 'unikit'
```

<Playground>
    <Flex bg="background" p={30} h={500} align="center" content="space-around">
        <Tabs options={['Tab 1', 'Tab 2', 'Tab 3']} />
        <Tabs activeColor="primary" indicatorSize={3} options={['Tab 1', 'Tab 2', 'Tab 3']} />
        <Tabs ripple activeColor="primary" indicatorSize={3} options={['Tab 1', 'Tab 2', 'Tab 3']} />
        <Box w={150} h={200}>
            <Tabs vertical tabSize={150} activeColor="primary" indicatorSize={3} options={['Tab 1', 'Tab 2', 'Tab 3']} />
        </Box>
    </Flex>
</Playground>

### Props

<Props of={Tabs} />